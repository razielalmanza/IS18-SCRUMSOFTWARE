-- Caso de uso 
-- 	    Registrar

-- Usando la BD ForoCiencias
-- y un esquema de la misma, modeloForo

-- Insertando en la tabla usuario

insert into usuario(nombreUsuario, correoCiencias, password, genero, fechaNac)values(
	val1,
	val2,
	val3,
	val4
	);
